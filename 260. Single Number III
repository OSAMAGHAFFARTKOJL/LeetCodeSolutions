class Solution:
    def singleNumber(self, nums: List[int]) -> List[int]:
        res=[]
        for num in nums :
            if num in res:
                res.remove (num)
            else:
                res.append(num)
        return res
